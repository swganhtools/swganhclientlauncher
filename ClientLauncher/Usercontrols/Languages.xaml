<UserControl x:Class="ClientLauncher.Usercontrols.Languages"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="Auto" Height="Auto">
    <Grid Margin="4,29,4,4">
        <Border BorderThickness="4" Background="{DynamicResource DarkAreaFill}" BorderBrush="{DynamicResource LightFill}" Margin="4" Padding="2" CornerRadius="{DynamicResource BigCornerRadius}">
		  	<Grid Margin="4">
				
                <WrapPanel Orientation="Vertical" x:Name="wpLanguages"/>
            </Grid>
              
			
		  	
		  </Border> 
    </Grid>
</UserControl>
